TODO List

Get the logger working
Make an IMU object
Change the example dashboard code to use ButtonColor instead of ButtonLED1 and ButtonLED2
Make the dashboard object
Get the Pi to run gpds on startup (sudo gpsd /dev/ttyAMA0 -F /var/run/gpsd.sock)
Get the Pi to run code on startup

organize code
push to GitHub